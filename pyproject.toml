[build-system]
requires = ["setuptools>=68", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "metering"
dynamic = ["version"]
description = "Pure tools for measuring information in finite discrete distributions"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.urls]
Homepage = "https://github.com/muchmirul/metering"
Documentation = "https://github.com/muchmirul/metering/blob/main/docs/theory.md"
Repository = "https://github.com/muchmirul/metering.git"
Releases = "https://github.com/muchmirul/metering/releases"

[project.optional-dependencies]
test = ["pytest>=8,<9"]

[project.scripts]
metering = "metering.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools_scm]
fallback_version = "0+untagged"
